import { Analytics } from '@vercel/analytics/next'
import type { Metadata, Viewport } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'Warrior Fitness (Gym)',
  description: 'Warrior Fitness (Gym): Personal Training, Fitness Training, Body Building, Weight Fitting, Weight Gain, Weight Loss, Nutrition Solution and Cardio.',
  keywords: ['Warrior Fitness (Gym)', 'Personal Training', 'Fitness Training', 'Body Building', 'Cardio'],
  openGraph: {
    title: 'Warrior Fitness (Gym)',
    description: 'Personal Training, Fitness Training, Body Building, Weight Fitting, Weight Gain, Weight Loss, Nutrition Solution and Cardio.',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  colorScheme: 'light dark',
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: 'white' },
    { media: '(prefers-color-scheme: dark)', color: 'black' },
  ],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
